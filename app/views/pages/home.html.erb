<%#   <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #f8f9fa url('/assets/background-image.jpg') center/cover no-repeat;
      font-family: Arial, sans-serif;
    }

    .container {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      height: 100vh;
      padding: 20px;
      text-align: center;
    }

    h1, p {
      color: #333333;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #ffffff;
      text-decoration: none;
      border-radius: 5px;
      transition: background-color 0.3s ease;
      margin-top: 20px;
    }

    .btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body> %>
  <div class="container">
    <h1>Bienvenue sur My Van Life</h1>
    <p>Explorez et réservez des vans pour vos aventures.</p>
    <%= link_to 'Voir les offres de vans', vans_path, class: "btn" %>
    <% if @user_signed_in %>
      <%= link_to 'Ajouter un Van', new_van_path, class: "btn" %>
    <% end %>
  </div>
